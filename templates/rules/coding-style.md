# Coding Style Rules

- TypeScript strict mode 사용
- 2-space indent
- 함수는 한 가지 일만 수행
- 변수명은 의미 있게 (i, j, temp 지양)
- 매직 넘버 금지 — 상수로 추출
- 불필요한 주석 금지 — 코드가 스스로 설명하게
- 사용하지 않는 import/변수 제거
- any 타입 사용 금지 (unknown 사용)
